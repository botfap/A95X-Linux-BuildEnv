--- webkit-1.9.6.orig/Source/WTF/wtf/Platform.h	2012-08-06 01:17:43.000000000 -0700
+++ webkit-1.9.6/Source/WTF/wtf/Platform.h	2014-03-07 16:15:47.656273017 -0800
@@ -294,7 +294,7 @@
 #endif
 
 #if CPU(ARM_NEON) && (!COMPILER(GCC) || GCC_VERSION_AT_LEAST(4, 7, 0))
-// All NEON intrinsics usage can be disabled by this macro.
+/* All NEON intrinsics usage can be disabled by this macro. */
 #define HAVE_ARM_NEON_INTRINSICS 1
 #endif
 
